# The "Separate" badge uses the app primary color. To make it turquoise, set theme.primaryColor in .streamlit/config.toml (e.g. #20C997)

[theme]
primaryColor = "#5B8CFF"
backgroundColor = "#F7F8FB"
secondaryBackgroundColor = "#FFFFFF"
textColor = "#0B1220"
font = "sans serif"
